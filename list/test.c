int main()
{
	SeqList<int> list(50);
	list.input();
	list.output();
	list.Locate(3);
	//cout << list.GetData(3);
	//int a = 10;
	//cout << "seach 10 is exist" << list.Search(a) << endl;
	//cout << "the seqlist's size is " << list.Size() << endl;
	//list.IsEmpty();
	//list.IsFull();

	//list.Insert(3,10);
	//list.output();
	//list.Remove(3);
	//list.output();

	return 0;
}